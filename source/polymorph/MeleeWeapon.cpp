#include "MeleeWeapon.h"
  
  MeleeWeapon::MeleeWeapon() : Weapon() {
  }

  MeleeWeapon::MeleeWeapon(TEquipablePrototype &prototype, unsigned char*name, unsigned char* manufacturer, int level): Weapon(TEquipablePrototype &prototype, char*name, char* manufacturer, int level) {
  }
  
  MeleeWeapon::~Weapon() {
  }
  
